
export default function Settings() {
  return (
    <div>
      <h2 className="text-xl font-semibold">Settings</h2>
      <div className="mt-4 space-y-3">
        <div className="glass p-4 rounded">Profile & security settings (biometric mock)</div>
        <div className="glass p-4 rounded">Guardian management (UI only)</div>
        <div className="glass p-4 rounded">Network selection (BlockDAG testnet)</div>
      </div>
    </div>
  );
}
